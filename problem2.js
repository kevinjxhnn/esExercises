// Use the const keyword in an example function

const printObject = () => {
    const tempObject = {
        country : "India",
        capital : "New Delhi"
    }

    console.log(tempObject)
}

printObject()