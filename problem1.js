// Use the let keyword in an example function

const printObject = () => {
    let tempObject = {
        name : "Kevin",
        company : "Mountblue"
    }

    console.log(tempObject)
}

printObject()
